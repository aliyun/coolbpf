

// store imm into stack
// kprobe:tcp_sendmsg { map[0] = 1; }

// store reg int stack
// load imm from stack
// kprobe:tcp_sendmsg { map[0] = sk; }


// alu64x
kprobe:tcp_sendmsg { map[0] = ctx->r8 + ctx->r9; }