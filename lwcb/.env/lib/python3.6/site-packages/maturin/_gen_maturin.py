
import os
import sys

def run():
    path = os.path.split(__file__)[0]
    file = os.path.join(path, 'maturin')
    if os.path.isfile(file):
        os.execv(file, sys.argv)
    else:
        raise RuntimeError("can't find " + file)
