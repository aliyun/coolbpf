# AgentSight 常见踩坑记录

> 仅记录读代码不容易发现的运行时/外部系统行为。代码可推导的问题不在此记录。

| # | 坑 | 原因 | 正确做法 |
|---|-----|------|----------|
| 1 | 创建 `/etc/agentsight/config.json` 后某些 Agent 不再被发现 | 用户配置文件**完全替换**内嵌默认规则，而非追加。只写了部分规则（如只配了 `cosh`）会丢失默认的其他 Agent 规则（如 `*claude*`） | 先复制完整的默认配置（见项目根目录 `agentsight.json`）再修改 |
